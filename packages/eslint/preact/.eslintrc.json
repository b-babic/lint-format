{
  "extends": ["@progressivestudio/eslint-config-base", "preact"], // extend base + add preact based config
  "settings": {
    // Add support for preact
    "react": {
      "version": "preact",
      "pragma": "h"
    }
  }
}
