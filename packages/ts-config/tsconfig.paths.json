{
  "compilerOptions": {
    "baseUrl": ".", // set base directory
    "paths": {
      "@/*": ["src/*"] // add default @ folder alias [@/modules/, @/components]
    } // set lookup locations and folder aliases
  }
}
